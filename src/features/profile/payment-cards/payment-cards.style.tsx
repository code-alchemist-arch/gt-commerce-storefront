import styled from "styled-components";
import { themeGet } from "@styled-system/theme-get";

export const PaymentCardsWrapper = styled.div`
  background: #ffffff;
  box-shadow: 0px 4px 22px rgb(0 0 0 / 3%);
  padding: 15px 25px 25px 25px;
`;
